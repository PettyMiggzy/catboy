{
  "name": "stag-locker",
  "version": "0.1.0",
  "description": "Universal token & LP locker for Robinhood Chain",
  "license": "MIT",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network robinhood"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "5.0.2",
    "hardhat": "^2.22.0"
  }
}
